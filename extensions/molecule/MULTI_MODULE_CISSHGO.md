# Multi-module CISSHGO (stub)

Long-form guidance and scenario matrices live in the Cursor skill bundle and in
`extensions/molecule/README.md`. Use those sources instead of duplicating content here.

- Skill: `.cursor/skills/cisco-ios-molecule-cisshgo-integration/SKILL.md`
- Layout and scenario list: `extensions/molecule/README.md`
- Architecture: `extensions/molecule/ARCHITECTURE.md`

Run scenarios from `cisco/ios/extensions` with `molecule test -s <scenario_name>`.
