---
- name: Assert that the result error equals command_timeout error
  ansible.builtin.assert:
    that:
      - result.module_stderr is defined
      - result.module_stderr == stderr_command_timeout

- name: Wait 30 secs for device to become reachable again
  ansible.builtin.wait_for:
    timeout: 30